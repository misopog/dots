#!/bin/sh

alias a="alias"

a ls="ls --color=auto"
a l="ls -l"
a la="ls -la"
a ll="ls -laH"
a code="vscodium"
a vim="nvim"
a se="sudoedit"
a jc="journalctl -xe"
a jcu="journalctl -xeu"
a sc="systemctl"
a ssc="sudo systemctl"
a aedit="$EDITOR ~/.aliasrc"
a asrc="source ~/.aliasrc"
a screen="maim ~/pix/screen-$(date +%H:%M:%S_%d.%m).png"
a xres="xrdb --merge ~/.Xresources"
a vim="nvim"
a n="nvim"

